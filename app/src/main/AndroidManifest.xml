<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PapaGiorgiosRestaurant"
        tools:targetApi="31">
        <activity
            android:name=".driver.ui.ConfirmedOrderActivity"
            android:exported="false" />
        <activity
            android:name=".driver.ui.OrderListActivity"
            android:exported="false" />
        <activity
            android:name=".client.ui.CartActivity"
            android:exported="false" />
        <activity
            android:name=".client.ui.MenuListActivity"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".client.ui.MenuListActivity" -->
        <!-- android:exported="false" -->
        <!-- android:theme="@style/Theme.PapaGiorgiosRestaurant" -->
        <!-- tools:ignore="DuplicateActivity" /> -->
        <activity
            android:name=".client.ui.RestaurantListActivity"
            android:exported="false" />
        <activity
            android:name=".common.authentication.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".common.authentication.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>